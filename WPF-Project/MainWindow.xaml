<Window x:Class="WPF_Project.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Project"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Button Click="OpenType" Margin="20 0"  Grid.Column="0">Marka</Button>
            <Button Click="OpenEngine"  Margin="10 0" Grid.Column="1">Silnik</Button>
            <TextBox Margin="20 0" Grid.Column="2" IsReadOnly="True" Background="#eee" x:Name="price"/>
            <Button Click="ShowPrice" Grid.Column="2" Grid.Row="1">Pokaż cenę</Button>
        </Grid>
    </StackPanel>
</Window>
